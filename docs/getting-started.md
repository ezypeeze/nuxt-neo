# Getting Started

```bash
yarn add nuxt-neo
```

Or NPM:

```bash
npm i nuxt-neo
```

Then add the module to `nuxt.config.js`:

```js
{
  modules: [
    ['nuxt-neo', {
      // Options
    }]
  ]
}
```


