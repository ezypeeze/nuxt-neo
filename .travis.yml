language: node_js
sudo: false
node_js:
    - "10"
install:
    - npm install
deploy:
  - provider: npm
    api_key: ${NPM_API_KEY}
    email: ${NPM_EMAIL_ADDRESS}
    skip_cleanup: true
    on:
      repo: ezypeeze/nuxt-neo
      tags: true
